<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Gradient biru -->
    <gradient
        android:angle="0"
        android:startColor="@color/wp_gradientErrorStart"
        android:endColor="@color/wp_gradientErrorEnd"
        android:type="linear" />

    <!-- Sudut rounded -->
    <corners android:radius="@dimen/_24sdp" />

    <!-- Shadow kecil via padding + elevation di button -->
    <padding android:left="@dimen/_16sdp" android:top="@dimen/_8sdp"
        android:right="@dimen/_16sdp" android:bottom="@dimen/_8sdp"/>
</shape>