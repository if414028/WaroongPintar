<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Gradient biru -->
    <gradient
        android:angle="0"
        android:endColor="@color/wp_gradientErrorEnd"
        android:startColor="@color/wp_gradientErrorStart"
        android:type="linear" />

    <!-- Sudut rounded -->
    <corners android:radius="@dimen/_24sdp" />

    <!-- Shadow kecil via padding + elevation di button -->
    <padding
        android:bottom="12dp"
        android:left="16dp"
        android:right="16dp"
        android:top="12dp" />
</shape>